// re-export so the docusaurus build and the decap admin page
// share one catalog (static/admin/icon-catalog.js).
export {
  MATERIAL_ICONS,
  LUCIDE_ICONS,
  DEFAULT_COLORS,
  SIZES,
} from '../../static/admin/icon-catalog.js';
